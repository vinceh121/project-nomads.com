---
import { gettext } from "../i18n";

const key = (await Astro.slots.render("default"))
  .trim()
  .replaceAll(/\s+/g, " ");
const value = gettext(Astro.currentLocale, key);
---

<Fragment set:html={value} />
